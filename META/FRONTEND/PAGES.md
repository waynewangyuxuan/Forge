# Pages

> **核心思想**：清晰的页面层级，侧边栏导航，版本上下文固定

---

## 一、页面清单

| 页面 | 路由 | 主要功能 | Sidebar |
|------|------|---------|---------|
| 项目列表 | `/projects` | 所有项目卡片、创建入口 | ❌ |
| 项目概览 | `/projects/:id` | 开发和运行状态概览、版本切换 | ✅ |
| Spec 编辑 | `/projects/:id/spec` | 编写 PRODUCT.md / TECHNICAL.md | ✅ |
| Review | `/projects/:id/review` | 审核 TODO.md、添加 feedback | ✅ |
| 执行进度 | `/projects/:id/execute` | 查看执行进度、任务列表 | ✅ |
| 运行管理 | `/projects/:id/runtime` | 配置触发方式、查看历史 | ✅ |
| Dashboard | `/projects/:id/dashboard` | 展示指标、日志 | ✅ |
| 全局设置 | `/settings` | 全局配置、凭证管理 | ❌ |

---

## 二、整体布局

### 2.1 两种布局

**无 Sidebar（列表页、设置页）**：

```
┌─────────────────────────────────────────────────────────────────┐
│  Header                                                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Main Content (全宽)                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**有 Sidebar（项目内页面）**：

```
┌─────────────────────────────────────────────────────────────────┐
│  Header                                                         │
├───────────────┬─────────────────────────────────────────────────┤
│               │                                                 │
│   Sidebar     │   Main Content                                  │
│   (200px)     │                                                 │
│               │                                                 │
└───────────────┴─────────────────────────────────────────────────┘
```

### 2.2 Header

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 Forge                                       [⚙️ Settings]   │
└─────────────────────────────────────────────────────────────────┘
```

- 左侧：Logo + 应用名
- 右侧：设置入口

### 2.3 Sidebar

```
┌───────────────┐
│               │
│  Kindle-Anki  │  ← 项目名
│  [v2.0 ▼]     │  ← 版本选择器
│               │
│  ─────────    │
│               │
│  📋 Overview  │  ← 开发相关
│  📝 Spec      │
│  👀 Review    │
│  ▶️ Execute   │
│               │
│  ─────────    │
│               │
│  ⚡ Runtime    │  ← 运行相关
│  📊 Dashboard │
│               │
│  ─────────    │
│               │
│  [← Back]     │  ← 返回项目列表
│               │
└───────────────┘
```

**分组**：
- 开发流：Overview / Spec / Review / Execute
- 运行流：Runtime / Dashboard

**版本切换**：
- 在 Sidebar 顶部
- 切换后整个项目上下文变为该版本
- 子页面 URL 不变，但数据刷新

---

## 三、各页面设计

### 3.1 项目列表 `/projects`

**功能**：展示所有项目，提供创建入口

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 Forge                                       [⚙️ Settings]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  My Projects                                    [+ New Project] │
│                                                                 │
│  ┌─────────────────────────┐  ┌─────────────────────────┐      │
│  │  Kindle → Anki    v2.0  │  │  Daily Digest      v1.0 │      │
│  │                         │  │                         │      │
│  │  Dev: ✅ Completed       │  │  Dev: 🔄 Executing      │      │
│  │  Run: ⏰ 9am daily       │  │  Run: ⏸️ Not configured │      │
│  │                         │  │                         │      │
│  │  [Open]     [VSCode]    │  │  [Open]     [VSCode]    │      │
│  └─────────────────────────┘  └─────────────────────────┘      │
│                                                                 │
│  ┌─────────────────────────┐                                   │
│  │  QuickCapture     v1.0  │                                   │
│  │                         │                                   │
│  │  Dev: 📝 Drafting       │                                   │
│  │  Run: —                 │                                   │
│  │                         │                                   │
│  │  [Open]     [VSCode]    │                                   │
│  └─────────────────────────┘                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**组件**：
- Header
- ProjectCard[]（网格布局，2-3 列）
- Button（New Project）
- Modal（Create Project 表单）

**交互**：
- 点击 [Open] → 跳转到项目概览
- 点击 [VSCode] → 调用 shell:openInEditor
- 点击 [+ New Project] → 打开创建弹窗

**CreateProjectModal**：

```
┌─────────────────────────────────────────────────────────┐
│  Create New Project                              [×]    │
│                                                         │
│  Project Name                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │ kindle-anki                                     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ✓ Create GitHub repository                             │
│    Repository: waynewang/kindle-anki                    │
│    Added to GitHub Project: "forge"                     │
│                                                         │
│                          [Cancel]  [Create Project]     │
└─────────────────────────────────────────────────────────┘
```

- 只需输入项目名称
- 自动创建 GitHub 仓库
- 自动 clone 到配置的 clone root 目录
- 自动添加到 GitHub Project "forge"

---

### 3.2 项目概览 `/projects/:id`

**功能**：展示开发状态和运行状态的概览，提供版本管理

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 Forge                                       [⚙️ Settings]   │
├───────────────┬─────────────────────────────────────────────────┤
│               │                                                 │
│  Kindle-Anki  │  Overview                                       │
│  [v2.0 ▼]     │                                                 │
│               │  ┌─────────────────────────────────────────┐   │
│  ─────────    │  │  Development                            │   │
│               │  │                                         │   │
│  📋 Overview  │  │  Status: ✅ Completed                    │   │
│  📝 Spec      │  │  Version: v2.0 (main branch)            │   │
│  👀 Review    │  │  Tasks: 45/45 done                      │   │
│  ▶️ Execute   │  │                                         │   │
│               │  │  [Iterate] [Open in VSCode]             │   │
│  ─────────    │  └─────────────────────────────────────────┘   │
│               │                                                 │
│  ⚡ Runtime    │  ┌─────────────────────────────────────────┐   │
│  📊 Dashboard │  │  Runtime                                │   │
│               │  │                                         │   │
│               │  │  Trigger: ⏰ Scheduled (0 9 * * *)       │   │
│               │  │  Last Run: 2h ago ✅                     │   │
│               │  │  Next Run: Tomorrow 9:00 AM             │   │
│               │  │                                         │   │
│               │  │  [Run Now] [Configure] [View Logs]      │   │
│               │  └─────────────────────────────────────────┘   │
│               │                                                 │
│  ─────────    │  Versions                                       │
│  [← Back]     │  ┌─────────────────────────────────────────┐   │
│               │  │ v2.0  main     ✅ Active    [Switch]    │   │
│               │  │ v1.0  release  ✅ Completed             │   │
│               │  └─────────────────────────────────────────┘   │
│               │                                                 │
└───────────────┴─────────────────────────────────────────────────┘
```

**组件**：
- Sidebar
- Card（Development 概览）
- Card（Runtime 概览）
- VersionList
- Badge（状态标签）
- Button

**开发卡片显示内容**：
- 当前状态（Drafting / Reviewing / Executing / Completed / Error）
- 版本号和 Git branch
- 任务进度（如果在执行中）
- 操作按钮（根据状态不同）

**运行卡片显示内容**：
- 触发方式（Manual / Scheduled / Always Running）
- 上次运行时间和结果
- 下次运行时间（如果是 Scheduled）
- 操作按钮

---

### 3.3 Spec 编辑 `/projects/:id/spec`

**功能**：编写 PRODUCT.md 和 TECHNICAL.md

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 Forge                                       [⚙️ Settings]   │
├───────────────┬─────────────────────────────────────────────────┤
│               │                                                 │
│  Kindle-Anki  │  Spec                          [Save] [Generate]│
│  v2.0         │                                                 │
│               │  [PRODUCT.md] [TECHNICAL.md]    ← Tabs          │
│  ─────────    │                                                 │
│               │  ┌─────────────────────────────────────────┐   │
│  📋 Overview  │  │                                         │   │
│  📝 Spec ←    │  │  # Kindle → Anki                        │   │
│  👀 Review    │  │                                         │   │
│  ▶️ Execute   │  │  ## 功能                                │   │
│               │  │  - 读取 Kindle highlights               │   │
│  ─────────    │  │  - 生成 Anki 卡片                       │   │
│               │  │  - 导出 CSV                             │   │
│  ⚡ Runtime    │  │                                         │   │
│  📊 Dashboard │  │  ## 用户流程                            │   │
│               │  │  1. 导入 highlights 文件                │   │
│               │  │  2. 选择要转换的书籍                     │   │
│               │  │  3. ...                                 │   │
│               │  │                                         │   │
│  ─────────    │  │  (MarkdownEditor)                       │   │
│  [← Back]     │  │                                         │   │
│               │  └─────────────────────────────────────────┘   │
│               │                                                 │
└───────────────┴─────────────────────────────────────────────────┘
```

**组件**：
- Sidebar
- Tabs（PRODUCT.md / TECHNICAL.md 切换）
- MarkdownEditor（textarea）
- Button（Save / Generate Scaffold）

**交互**：
- Tab 切换时保存当前内容
- [Save] → 保存到文件
- [Generate] → 触发 Scaffold 生成 → 完成后跳转 Review

**状态**：
- 未保存时 Save 按钮高亮
- Generate 时显示 loading 状态

---

### 3.4 Review `/projects/:id/review`

**功能**：审核 AI 生成的 TODO.md，添加反馈

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 Forge                                       [⚙️ Settings]   │
├───────────────┬─────────────────────────────────────────────────┤
│               │                                                 │
│  Kindle-Anki  │  Review TODO                                    │
│  v2.0         │                                                 │
│               │  ┌─────────────────────────────────────────┐   │
│  ─────────    │  │                                         │   │
│               │  │  ## Milestone 1: Setup                  │   │
│  📋 Overview  │  │                                         │   │
│  📝 Spec      │  │  - [ ] 001. Initialize project          │   │
│  👀 Review ←  │  │  - [ ] 002. Add dependencies            │   │
│  ▶️ Execute   │  │  - [ ] 003. Create config               │   │
│               │  │                                         │   │
│  ─────────    │  │  ## Milestone 2: Core                   │   │
│               │  │                                         │   │
│  ⚡ Runtime    │  │  - [ ] 004. Parse highlights            │   │
│  📊 Dashboard │  │  - [ ] 005. Generate cards              │   │
│               │  │  ...                                    │   │
│               │  │                                         │   │
│               │  │  (MarkdownEditor, 可直接编辑)            │   │
│  ─────────    │  └─────────────────────────────────────────┘   │
│  [← Back]     │                                                 │
│               │  💬 Feedback                                    │
│               │  ┌─────────────────────────────────────────┐   │
│               │  │  请把 Milestone 2 拆分得更细...          │   │
│               │  └─────────────────────────────────────────┘   │
│               │                                                 │
│               │           [Regenerate] [Clear] [Approve →]      │
│               │                                                 │
└───────────────┴─────────────────────────────────────────────────┘
```

**组件**：
- Sidebar
- MarkdownEditor（显示和编辑 TODO.md）
- FeedbackPanel（整体反馈输入）
- Button（Regenerate / Clear / Approve）

**交互**：
- 可直接编辑 TODO.md 内容
- Feedback 输入框用于整体反馈
- [Regenerate] → 基于 feedback 重新生成 → 刷新 TODO.md
- [Clear] → 清除 feedback
- [Approve] → 确认通过 → 状态变为 Ready → 可选跳转到 Execute

**MVP 简化**：
- 不做行级定位
- 不做 suggest edit
- 只支持整体 comment

---

### 3.5 执行进度 `/projects/:id/execute`

**功能**：查看代码生成的执行进度

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 Forge                                       [⚙️ Settings]   │
├───────────────┬─────────────────────────────────────────────────┤
│               │                                                 │
│  Kindle-Anki  │  Execution                     Status: Running  │
│  v2.0         │                                                 │
│               │  Progress                                       │
│  ─────────    │  ████████████░░░░░░░░░░░░░░░░░░░░  23/45       │
│               │                                                 │
│  📋 Overview  │  Current Task                                   │
│  📝 Spec      │  ┌─────────────────────────────────────────┐   │
│  👀 Review    │  │  ● 023. Implement card generation       │   │
│  ▶️ Execute ← │  │    Depends: 021, 022                    │   │
│               │  └─────────────────────────────────────────┘   │
│  ─────────    │                                                 │
│               │  Task List                                      │
│  ⚡ Runtime    │  ┌─────────────────────────────────────────┐   │
│  📊 Dashboard │  │  ## Milestone 1: Setup                  │   │
│               │  │  ✓ 001. Initialize project              │   │
│               │  │  ✓ 002. Add dependencies                │   │
│               │  │  ✓ 003. Create config                   │   │
│               │  │                                         │   │
│  ─────────    │  │  ## Milestone 2: Core                   │   │
│  [← Back]     │  │  ✓ 021. Parse highlights                │   │
│               │  │  ✓ 022. Extract metadata                │   │
│               │  │  ● 023. Implement card generation  ← 当前│   │
│               │  │  ○ 024. Format output                   │   │
│               │  └─────────────────────────────────────────┘   │
│               │                                                 │
│               │                    [Pause] [Open in VSCode]     │
│               │                                                 │
└───────────────┴─────────────────────────────────────────────────┘
```

**组件**：
- Sidebar
- Badge（状态）
- ProgressBar（整体进度）
- Card（当前任务）
- TaskList（任务列表，按 Milestone 分组）
- Button（操作按钮）

**状态和按钮**：

| 状态 | 显示 | 按钮 |
|------|------|------|
| Ready | 等待开始 | [Start] |
| Running | 进度条 + 当前任务 | [Pause] [VSCode] |
| Paused | 暂停提示 | [Resume] [Abort] |
| Error | 错误信息 | [Retry] [Skip] [VSCode] |
| Completed | 完成提示 | [Configure Runtime →] |

**TaskList 状态图标**：

| 状态 | 图标 |
|------|------|
| pending | ○ 空心圆 |
| running | ● amber spinner |
| completed | ✓ 绿色勾 |
| failed | ✗ 红色叉 |
| skipped | — 灰色横线 |

---

### 3.6 运行管理 `/projects/:id/runtime`

**功能**：配置运行触发方式，查看运行历史

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 Forge                                       [⚙️ Settings]   │
├───────────────┬─────────────────────────────────────────────────┤
│               │                                                 │
│  Kindle-Anki  │  Runtime                                        │
│  v2.0         │                                                 │
│               │  Configuration                                  │
│  ─────────    │  ┌─────────────────────────────────────────┐   │
│               │  │  Trigger Mode                           │   │
│  📋 Overview  │  │  ○ Manual only                          │   │
│  📝 Spec      │  │  ● Scheduled                            │   │
│  👀 Review    │  │    └─ Cron: [0 9 * * *]                 │   │
│  ▶️ Execute   │  │  ○ Always running                       │   │
│               │  │                                         │   │
│  ─────────    │  │  Credentials                            │   │
│               │  │  NOTION_API: [notion-main ▼]            │   │
│  ⚡ Runtime ←  │  │  ANKI_URL:   [http://localhost:8765]   │   │
│  📊 Dashboard │  │                                         │   │
│               │  │                     [Save Configuration]│   │
│               │  └─────────────────────────────────────────┘   │
│  ─────────    │                                                 │
│  [← Back]     │  Run History                        [Run Now]   │
│               │  ┌─────────────────────────────────────────┐   │
│               │  │  Today 9:00 AM    ✅ Success    23s     │   │
│               │  │  Yesterday 9:00   ✅ Success    21s     │   │
│               │  │  2 days ago       ❌ Failed     12s     │   │
│               │  │                                  [Logs] │   │
│               │  └─────────────────────────────────────────┘   │
│               │                                                 │
└───────────────┴─────────────────────────────────────────────────┘
```

**组件**：
- Sidebar
- Card（Configuration）
- RadioGroup（Trigger Mode）
- Input（Cron 表达式）
- Select（Credential 选择）
- RunHistoryList
- Button

**Trigger Mode**：

| 模式 | 说明 | 额外输入 |
|------|------|---------|
| Manual only | 只能手动触发 | 无 |
| Scheduled | 定时触发 | Cron 表达式 |
| Always running | 持续运行 | 无 |

**Credentials 配置**：
- 显示项目需要的环境变量
- 可选择全局凭证或输入项目特定值

---

### 3.7 Dashboard `/projects/:id/dashboard`

**功能**：展示运行指标和日志

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 Forge                                       [⚙️ Settings]   │
├───────────────┬─────────────────────────────────────────────────┤
│               │                                                 │
│  Kindle-Anki  │  Dashboard                          [Run Now]   │
│  v2.0         │                                                 │
│               │  ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │
│  ─────────    │  │     847     │ │      12     │ │    23s    │ │
│               │  │ Cards Today │ │ Books Done  │ │ Last Run  │ │
│  📋 Overview  │  └─────────────┘ └─────────────┘ └───────────┘ │
│  📝 Spec      │                                                 │
│  👀 Review    │  Cards Generated (Last 7 days)                  │
│  ▶️ Execute   │  ┌─────────────────────────────────────────┐   │
│               │  │    ╭─╮                                  │   │
│  ─────────    │  │ ╭──╯ ╰──╮    ╭──╮                       │   │
│               │  │─╯       ╰────╯  ╰──                     │   │
│  ⚡ Runtime    │  └─────────────────────────────────────────┘   │
│  📊 Dashboard←│                                                 │
│               │  Recent Logs                                    │
│               │  ┌─────────────────────────────────────────┐   │
│  ─────────    │  │ 09:00:01 Starting Kindle sync...        │   │
│  [← Back]     │  │ 09:00:03 Found 3 new highlights         │   │
│               │  │ 09:00:15 Generated 12 cards             │   │
│               │  │ 09:00:16 Completed in 23s ✓             │   │
│               │  └─────────────────────────────────────────┘   │
│               │                                                 │
└───────────────┴─────────────────────────────────────────────────┘
```

**组件**：
- Sidebar
- StatCard[]（指标卡片）
- Chart（趋势图，MVP 可选）
- LogViewer（最近日志）
- Button（Run Now）

**指标来源**：
- 从 DASHBOARD.yaml 读取配置
- 解析 logs/ 目录下的日志提取数据

**MVP 简化**：
- 图表可以先不做，只显示 StatCard
- 日志显示最近一次运行的输出

---

### 3.8 全局设置 `/settings`

**功能**：管理 GitHub 连接、全局配置和凭证

**布局**：

```
┌─────────────────────────────────────────────────────────────────┐
│  🔥 Forge                                       [⚙️ Settings]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ← Back to Projects                                             │
│                                                                 │
│  Settings                                                       │
│                                                                 │
│  GitHub                                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Status: ✅ Connected as @waynewang                      │   │
│  │                                         [Disconnect]     │   │
│  │                                                         │   │
│  │  Clone Location                                          │   │
│  │  [~/Projects]                              [Browse]     │   │
│  │  Projects will be cloned to this directory               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  General                                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Editor                                                 │   │
│  │  [Visual Studio Code ▼]                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Credentials                                        [+ Add]     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  notion-main     NOTION_API    ••••••••     [Edit] [🗑️] │   │
│  │  github-pat      GITHUB_TOKEN  ••••••••     [Edit] [🗑️] │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Claude                                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Status: ✅ Available (claude-sonnet-4)                  │   │
│  │  Allowed Tools: [Read, Write, Bash]                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**未连接 GitHub 时**：

```
│  GitHub                                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Status: ⚠️ Not connected                                │   │
│  │  Connect to GitHub to create and manage projects.       │   │
│  │                                       [Connect GitHub]   │   │
│  └─────────────────────────────────────────────────────────┘   │
```

**组件**：
- Card（GitHub 连接状态）
- Card（General 设置）
- Card（Credentials 列表）
- Card（Claude 状态）
- Input
- Select
- Button
- Modal（添加/编辑 Credential）

**GitHub 管理**：
- 显示 GitHub 认证状态和用户名
- Clone Location 配置（Browse 选择）
- 未认证时显示 [Connect GitHub] 按钮

**Credentials 管理**：
- 列表显示 nickname、type、masked value
- 支持添加、编辑、删除
- 实际值存储在系统 Keychain

---

## 四、路由与导航

### 4.1 路由配置

```typescript
// router.tsx

const routes = [
  {
    path: '/projects',
    element: <ProjectListPage />,
  },
  {
    path: '/projects/:projectId',
    element: <ProjectLayout />,  // 包含 Sidebar
    children: [
      { index: true, element: <OverviewPage /> },
      { path: 'spec', element: <SpecPage /> },
      { path: 'review', element: <ReviewPage /> },
      { path: 'execute', element: <ExecutePage /> },
      { path: 'runtime', element: <RuntimePage /> },
      { path: 'dashboard', element: <DashboardPage /> },
    ],
  },
  {
    path: '/settings',
    element: <SettingsPage />,
  },
  {
    path: '/',
    element: <Navigate to="/projects" />,
  },
]
```

**ProjectLayout**：

```typescript
// 包含 Sidebar 的布局组件
function ProjectLayout() {
  const { projectId } = useParams()
  
  return (
    <div className="flex">
      <Sidebar projectId={projectId} />
      <main className="flex-1">
        <Outlet />
      </main>
    </div>
  )
}
```

### 4.2 路由参数

| 参数 | 类型 | 说明 | 来源 |
|------|------|------|------|
| `projectId` | string (UUID) | 项目 ID | URL path |
| `versionId` | string (UUID) | 当前版本 ID | Store（不在 URL） |

**为什么 versionId 不在 URL？**

- 版本是项目的上下文，不是独立资源
- 切换版本不改变页面，只刷新数据
- 简化 URL，避免过长

**获取参数**：

```typescript
// 获取 projectId
const { projectId } = useParams<{ projectId: string }>()

// 获取当前版本（从 Store）
const currentVersionId = useServerStore(state => state.currentVersionId)
```

### 4.3 版本上下文管理

版本 ID 存储在 Server Store，与 projectId 关联：

```typescript
// stores/server.store.ts

interface ServerState {
  // 当前选中的版本（每个项目一个）
  currentVersionByProject: Record<string, string>  // projectId → versionId
}

// 获取当前项目的版本
function getCurrentVersionId(projectId: string): string | null {
  return state.currentVersionByProject[projectId] ?? null
}

// 切换版本
function setCurrentVersion(projectId: string, versionId: string) {
  state.currentVersionByProject[projectId] = versionId
}
```

**ProjectLayout 中初始化版本**：

```typescript
function ProjectLayout() {
  const { projectId } = useParams()
  const currentVersionId = useServerStore(
    state => state.currentVersionByProject[projectId!]
  )
  
  // 如果没有选中版本，加载项目后选中最新版本
  useEffect(() => {
    if (!currentVersionId && projectId) {
      loadProjectAndSelectLatestVersion(projectId)
    }
  }, [projectId, currentVersionId])
  
  if (!currentVersionId) {
    return <Loading />
  }
  
  return (
    <div className="flex">
      <Sidebar projectId={projectId!} versionId={currentVersionId} />
      <main className="flex-1">
        <Outlet context={{ projectId, versionId: currentVersionId }} />
      </main>
    </div>
  )
}
```

**子页面获取上下文**：

```typescript
// 使用 Outlet context
function useProjectContext() {
  return useOutletContext<{ projectId: string; versionId: string }>()
}

// 在页面中使用
function SpecPage() {
  const { projectId, versionId } = useProjectContext()
  // ...
}
```

### 4.4 导航守卫

**未保存提示**：

Spec 编辑页和 Review 页可能有未保存内容，离开前需要提示。

```typescript
// hooks/useUnsavedChanges.ts

function useUnsavedChanges(hasUnsaved: boolean) {
  const blocker = useBlocker(hasUnsaved)
  
  useEffect(() => {
    if (blocker.state === 'blocked') {
      const confirmed = window.confirm(
        'You have unsaved changes. Are you sure you want to leave?'
      )
      if (confirmed) {
        blocker.proceed()
      } else {
        blocker.reset()
      }
    }
  }, [blocker])
}

// 在 SpecPage 中使用
function SpecPage() {
  const [content, setContent] = useState('')
  const [savedContent, setSavedContent] = useState('')
  
  const hasUnsaved = content !== savedContent
  useUnsavedChanges(hasUnsaved)
  
  // ...
}
```

**执行中禁止离开**：

执行进度页在执行中时，离开可能导致问题。

```typescript
function ExecutePage() {
  const { versionId } = useProjectContext()
  const execution = useRealtimeStore(state => state.getActiveExecution(versionId))
  const isExecuting = execution?.status === 'running'

  // 执行中时提示
  useUnsavedChanges(isExecuting)

  // ...
}
```

### 4.5 错误处理

**404 页面**：

```typescript
// routes
{
  path: '*',
  element: <NotFoundPage />,
}
```

**项目不存在**：

```typescript
function ProjectLayout() {
  const { projectId } = useParams()
  const project = useServerStore(state => state.projects[projectId!])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<string | null>(null)
  
  useEffect(() => {
    loadProject(projectId!)
      .catch(err => setError(err.message))
      .finally(() => setLoading(false))
  }, [projectId])
  
  if (loading) return <Loading />
  if (error) return <ErrorPage message={error} />
  if (!project) return <NotFoundPage message="Project not found" />
  
  // ...
}
```

**错误边界**：

```typescript
// 包裹整个应用
<ErrorBoundary fallback={<ErrorPage />}>
  <RouterProvider router={router} />
</ErrorBoundary>
```

### 4.6 导航工具

封装常用导航操作：

```typescript
// hooks/useNavigation.ts

function useNavigation() {
  const navigate = useNavigate()
  
  return {
    // 回到项目列表
    toProjects: () => navigate('/projects'),
    
    // 进入项目
    toProject: (projectId: string) => navigate(`/projects/${projectId}`),
    
    // 项目内导航
    toSpec: (projectId: string) => navigate(`/projects/${projectId}/spec`),
    toReview: (projectId: string) => navigate(`/projects/${projectId}/review`),
    toExecute: (projectId: string) => navigate(`/projects/${projectId}/execute`),
    toRuntime: (projectId: string) => navigate(`/projects/${projectId}/runtime`),
    toDashboard: (projectId: string) => navigate(`/projects/${projectId}/dashboard`),
    
    // 设置
    toSettings: () => navigate('/settings'),
    
    // 返回
    back: () => navigate(-1),
  }
}
```

### 4.7 路由与状态同步

**进入项目时加载数据**：

```typescript
// ProjectLayout 负责加载项目基础数据
function ProjectLayout() {
  const { projectId } = useParams()
  
  useEffect(() => {
    // 加载项目和版本列表
    serverStore.loadProject(projectId!)
    serverStore.loadVersions(projectId!)
  }, [projectId])
  
  // ...
}
```

**离开项目时清理**：

```typescript
function ProjectLayout() {
  const { projectId } = useParams()
  
  useEffect(() => {
    return () => {
      // 清理实时订阅
      realtimeStore.unsubscribeAll(projectId!)
    }
  }, [projectId])
  
  // ...
}
```

---

## 五、页面状态流转

### 5.1 开发流页面流转

```
项目列表
    │
    │ [Open] 或 [+ New]
    ▼
项目概览 ◄────────────────────────┐
    │                            │
    │ 点击 Spec / Sidebar        │
    ▼                            │
Spec 编辑                        │
    │                            │
    │ [Generate Scaffold]        │
    ▼                            │
Review                           │
    │                            │
    │ [Approve]                  │
    ▼                            │
执行进度                          │
    │                            │
    │ 完成                        │
    ▼                            │
项目概览 ─────────────────────────┘
    │
    │ [Iterate] → 创建新版本 → 回到 Spec
    ▼
```

### 5.2 运行流页面流转

```
项目概览
    │
    │ [Configure] 或 Sidebar
    ▼
运行管理
    │
    │ [Save] 后
    ▼
项目概览
    │
    │ [Run Now] 或 [Dashboard]
    ▼
Dashboard
```

---

## 六、Checklist

| 检查项 | 说明 |
|--------|------|
| 每个页面有明确的入口和出口 | 用户不会迷路 |
| Sidebar 当前页高亮 | 用户知道在哪 |
| 版本切换后数据刷新 | 不会看到旧版本数据 |
| 操作按钮根据状态变化 | 不会出现无效按钮 |
| 长时间操作有 loading 状态 | 用户知道在等待 |
| 错误有明确提示 | 用户知道出了什么问题 |
| 未保存内容离开时有提示 | 防止数据丢失 |
| 项目不存在时显示 404 | 友好的错误处理 |
| 进入项目时加载数据 | 数据就绪才渲染页面 |
| 离开项目时清理订阅 | 避免内存泄漏 |
